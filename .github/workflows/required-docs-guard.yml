name: required-docs-guard
on:
  pull_request:
  push:
jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Ensure required docs exist
        run: |
          test -f docs/BUSINESS_OS_BLUEPRINT.md
