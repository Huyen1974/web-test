[pytest]
addopts = -q --maxfail=1 -m unit --cov=agent_data --cov-fail-under=80
markers =
    unit: marks tests as unit tests
    e2e: marks tests as end-to-end
    smoke: marks lightweight canary smoke tests
    fixture: marks tests related to fixture generation
    slow: marks tests that are slow to run
